{
  "name": "servicepos-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.58",
    "axios": "^0.21.4"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist --force",
    "test": "openapi lint"
  }
}
